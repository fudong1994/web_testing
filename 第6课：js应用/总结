web测试
1、点点点（鼠标操作，单击，双击，拖拽，悬停）
2、用户输入（用户名，单选，多选，select下拉框选择）
3、控件（日期），文件上传。
4、验证码
5、窗口滚动

各种各样的浏览器操作。selenium,cypress,playwright,puppeter
封装的浏览器操作都是类似的

面试题：报错无法定位元素，出现的原因可能有哪些？
-1、元素定位表达式是否正确，怎么确定？浏览器当中 ctrl+f，看能不能找到元素，是否唯一
-2、元素表达式正确。检查是否设置了等待。（隐性等待，sleep）
-3、元素是否在另一个窗口（driver.window_handles）
-4、在不在 iframe 当中。
-5、会动态变化的元素（1、表达式可能变化）

验证码：
1、先让开发关掉验证码
2、开发万能的验证码
3、第三方的解码平台，超级鹰

如何操作一个对象？
-方法
-属性，获取属性，修改属性

